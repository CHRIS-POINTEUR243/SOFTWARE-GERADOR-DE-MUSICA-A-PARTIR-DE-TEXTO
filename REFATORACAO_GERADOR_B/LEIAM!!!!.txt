-- PROGRAMA FUNCIONA COM TODOS OS ARQUIVOS RODANDO POR main.py
   SEM O ARQUIVO class_UI.py

-- O ARQUIVO class_UI.py É UM TESTE PARA RODAR O PROGRAMA
   PELO STREAMLIT COM A CLASSE UI (INTERFACE DE USUÁRIO)
   CHAMANDO A CLASSE MUSIC SERVICES.

-- APARENTEMENTE O PYGAME.MIDI NÃO FUNCIONA NO STREAMLIT
   PORQUE O STREAMLIT NÃO TEM PORTA MIDI, ENTÃO NÃO CONSEGUI
   FAZER SAIR SOM PELA INTERFACE

-- PELO QUE EU LI TEM QUE TRANSFORMAR O ARQUIVO MIDI GERADO
   EM .WAV OU .MP3 PARA PODER TOCAR DIRETO PELO STREAMLIT,
   USANDO st.audio()

-- PELO QUE EU VI AS BIBLIOTECAS MIDO E FLUIDSYNTH CONSEGUEM
   FAZER ISSO